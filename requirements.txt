# This one draws the app's window. The app needs it to work at all.
customtkinter>=5.2.2

# This one draws the soft glow/gradient pictures behind the timer and the
# Henshin button, and the faint background pattern. The app needs it to
# work at all now too.
Pillow>=10.0.0

# These help the app see which window you're looking at, on Windows.
# Without them, the app still works, but it can't tell what app you're in,
# so it can't block anything.
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0

# This shows little pop-up messages on Windows. Not required — if it's
# missing, the app shows its own message banner inside the window instead.
winotify>=1.1.0; sys_platform == "win32"

# This lets the app ask Claude for help on tricky windows it isn't sure
# about. Not required — the switch for this stays off until you install it
# and set up an ANTHROPIC_API_KEY. The app works fine without it.
anthropic>=0.121.0

# Only needed if you want to run the test suite.
pytest>=8.0.0